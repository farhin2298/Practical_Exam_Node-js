<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - TaskMaster</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body style="background: white;">
    <%- include('partials/navbar') %>

        <div class="auth-layout">
            <div class="form-container">
                <div style="text-align: center; margin-bottom: 2.5rem;">
                    <h1 style="font-weight: 800; font-size: 2.25rem; letter-spacing: -1.5px; margin-bottom: 0.5rem;">
                        Welcome back</h1>
                    <p style="color: var(--text-muted); font-weight: 500;">Please enter your details to continue.</p>
                </div>

                <% if (locals.error) { %>
                    <div
                        style="background: #fff1f2; border: 1px solid #ffe4e6; color: var(--danger); padding: 1rem; border-radius: var(--radius-sm); margin-bottom: 2rem; font-size: 0.9rem; font-weight: 700;">
                        <i class="fas fa-exclamation-circle"></i>
                        <%= error %>
                    </div>
                    <% } %>

                        <form action="/auth/login" method="POST">
                            <div class="form-group">
                                <label>USERNAME</label>
                                <input type="text" name="username" class="form-input" placeholder="Enter your username"
                                    required>
                            </div>
                            <div class="form-group" style="margin-bottom: 2rem;">
                                <label>PASSWORD</label>
                                <input type="password" name="password" class="form-input" placeholder="••••••••"
                                    required>
                            </div>
                            <button type="submit" class="btn btn-primary"
                                style="width: 100%; padding: 1.1rem; font-size: 1rem;">Sign In</button>
                        </form>

                        <p
                            style="text-align: center; margin-top: 2.5rem; color: var(--text-muted); font-size: 0.95rem; font-weight: 600;">
                            New to TaskMaster? <a href="/register"
                                style="color: var(--primary); text-decoration: none; font-weight: 800;">Create an
                                account</a>
                        </p>
            </div>
        </div>
</body>

</html>